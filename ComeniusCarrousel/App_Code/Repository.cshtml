@functions{
    public static class CRUD {
        public static void doSimpleIdUpdate(String table, String column, String value, String idName, int id) {
            var db = Database.Open(App.Database);
            db.Execute("UPDATE @0 SET @1 = @2 WHERE @3 = @4", table, column, value, idName, id);
            db.Close();
        }
    }

    public static List<string> SplitLeerlingen(string leerlingInvoer, List<string> leerlingen)
    {
        string leerling = leerlingInvoer.Substring(0, leerlingInvoer.IndexOf("\n"));
        leerlingen.Add(leerling);
        if (leerlingInvoer.Contains("\n"))
        {
            leerlingInvoer = leerlingInvoer.Remove(0, leerlingInvoer.IndexOf("\n") + 2);
        }
        else
        {
            return leerlingen;
        }
        SplitLeerlingen(leerlingInvoer, leerlingen);
        return leerlingen;
    }

    public static List<Tuple<string,string,int,string,string,string,string,bool>> SplitLeerling(string leerling)
    {
        return null;
    }
}